"""qa_verification — independent verification suite (DATASET_VERIFICATION_ARCHITECTURE).

Boundary rule: this package must NEVER import ``proced``. Every number is
recomputed from raw files with rasterio/cv2/shapely/pandas.
"""

__version__ = "1.0.0"
